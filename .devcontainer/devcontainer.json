{
  "name": "Claude Code Development Container",
  "dockerFile": "Dockerfile",
  "remoteUser": "node",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {},
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.vscode-typescript-next",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ]
    }
  },
  "postCreateCommand": "/bin/bash -c 'if [ -f init-firewall.sh ]; then chmod +x init-firewall.sh && ./init-firewall.sh; fi'",
  "containerEnv": {
    "ANTHROPIC_API_KEY": "${localEnv:ANTHROPIC_API_KEY}",
    "ANTHROPIC_AUTH_TOKEN": "${localEnv:ANTHROPIC_AUTH_TOKEN}",
    "ANTHROPIC_ORGANIZATION_ID": "${localEnv:ANTHROPIC_ORGANIZATION_ID}",
    "ANTHROPIC_PROJECT_ID": "${localEnv:ANTHROPIC_PROJECT_ID}",
    "ANTHROPIC_SESSION_ID": "${localEnv:ANTHROPIC_SESSION_ID}",
    "ANTHROPIC_SESSION_TOKEN": "${localEnv:ANTHROPIC_SESSION_TOKEN}",
    "CLAUDE_DESKTOP_URL": "${localEnv:CLAUDE_DESKTOP_URL}",
    "CLAUDE_DESKTOP_API_KEY": "${localEnv:CLAUDE_DESKTOP_API_KEY}",
    "CLAUDE_API_KEY": "${localEnv:CLAUDE_API_KEY}",
    "CLAUDE_SESSION_KEY": "${localEnv:CLAUDE_SESSION_KEY}",
    "CLAUDE_CLI_USER_ID": "${localEnv:CLAUDE_CLI_USER_ID}",
    "GH_TOKEN": "${localEnv:GH_TOKEN}",
    "GITHUB_TOKEN": "${localEnv:GITHUB_TOKEN}",
    "OPENAI_API_KEY": "${localEnv:OPENAI_API_KEY}",
    "AWS_ACCESS_KEY_ID": "${localEnv:AWS_ACCESS_KEY_ID}",
    "AWS_SECRET_ACCESS_KEY": "${localEnv:AWS_SECRET_ACCESS_KEY}",
    "AWS_REGION": "${localEnv:AWS_REGION}",
    "AZURE_OPENAI_API_KEY": "${localEnv:AZURE_OPENAI_API_KEY}",
    "AZURE_OPENAI_ENDPOINT": "${localEnv:AZURE_OPENAI_ENDPOINT}",
    "GOOGLE_API_KEY": "${localEnv:GOOGLE_API_KEY}",
    "GOOGLE_CLOUD_PROJECT": "${localEnv:GOOGLE_CLOUD_PROJECT}",
    "NETLIFY_AUTH_TOKEN": "${localEnv:NETLIFY_AUTH_TOKEN}",
    "VERCEL_TOKEN": "${localEnv:VERCEL_TOKEN}",
    "RAILWAY_TOKEN": "${localEnv:RAILWAY_TOKEN}",
    "HEROKU_API_KEY": "${localEnv:HEROKU_API_KEY}",
    "DIGITALOCEAN_TOKEN": "${localEnv:DIGITALOCEAN_TOKEN}",
    "SUPABASE_ACCESS_TOKEN": "${localEnv:SUPABASE_ACCESS_TOKEN}",
    "FIREBASE_TOKEN": "${localEnv:FIREBASE_TOKEN}",
    "HUGGINGFACE_TOKEN": "${localEnv:HUGGINGFACE_TOKEN}",
    "COHERE_API_KEY": "${localEnv:COHERE_API_KEY}",
    "REPLICATE_API_TOKEN": "${localEnv:REPLICATE_API_TOKEN}",
    "PINECONE_API_KEY": "${localEnv:PINECONE_API_KEY}",
    "WEAVIATE_API_KEY": "${localEnv:WEAVIATE_API_KEY}",
    "MILVUS_TOKEN": "${localEnv:MILVUS_TOKEN}",
    "QDRANT_API_KEY": "${localEnv:QDRANT_API_KEY}",
    "CHROMA_SERVER_AUTH_TOKEN": "${localEnv:CHROMA_SERVER_AUTH_TOKEN}",
    "API_KEY": "${localEnv:API_KEY}",
    "API_SECRET": "${localEnv:API_SECRET}",
    "API_TOKEN": "${localEnv:API_TOKEN}",
    "API_ENDPOINT": "${localEnv:API_ENDPOINT}",
    "API_URL": "${localEnv:API_URL}",
    "AUTH_TOKEN": "${localEnv:AUTH_TOKEN}",
    "ACCESS_TOKEN": "${localEnv:ACCESS_TOKEN}",
    "SESSION_TOKEN": "${localEnv:SESSION_TOKEN}",
    "DATABASE_URL": "${localEnv:DATABASE_URL}",
    "REDIS_URL": "${localEnv:REDIS_URL}",
    "MONGODB_URI": "${localEnv:MONGODB_URI}",
    "POSTGRES_URL": "${localEnv:POSTGRES_URL}",
    "MYSQL_URL": "${localEnv:MYSQL_URL}",
    "ELASTICSEARCH_URL": "${localEnv:ELASTICSEARCH_URL}",
    "RABBITMQ_URL": "${localEnv:RABBITMQ_URL}",
    "KAFKA_BROKERS": "${localEnv:KAFKA_BROKERS}",
    "SMTP_HOST": "${localEnv:SMTP_HOST}",
    "SMTP_PORT": "${localEnv:SMTP_PORT}",
    "SMTP_USER": "${localEnv:SMTP_USER}",
    "SMTP_PASS": "${localEnv:SMTP_PASS}",
    "EMAIL_FROM": "${localEnv:EMAIL_FROM}",
    "STRIPE_API_KEY": "${localEnv:STRIPE_API_KEY}",
    "STRIPE_SECRET_KEY": "${localEnv:STRIPE_SECRET_KEY}",
    "PAYPAL_CLIENT_ID": "${localEnv:PAYPAL_CLIENT_ID}",
    "PAYPAL_CLIENT_SECRET": "${localEnv:PAYPAL_CLIENT_SECRET}",
    "TWILIO_ACCOUNT_SID": "${localEnv:TWILIO_ACCOUNT_SID}",
    "TWILIO_AUTH_TOKEN": "${localEnv:TWILIO_AUTH_TOKEN}",
    "SENDGRID_API_KEY": "${localEnv:SENDGRID_API_KEY}",
    "MAILGUN_API_KEY": "${localEnv:MAILGUN_API_KEY}",
    "SLACK_TOKEN": "${localEnv:SLACK_TOKEN}",
    "SLACK_WEBHOOK_URL": "${localEnv:SLACK_WEBHOOK_URL}",
    "DISCORD_TOKEN": "${localEnv:DISCORD_TOKEN}",
    "DISCORD_WEBHOOK_URL": "${localEnv:DISCORD_WEBHOOK_URL}",
    "TELEGRAM_BOT_TOKEN": "${localEnv:TELEGRAM_BOT_TOKEN}",
    "TWITTER_API_KEY": "${localEnv:TWITTER_API_KEY}",
    "TWITTER_API_SECRET": "${localEnv:TWITTER_API_SECRET}",
    "FACEBOOK_APP_ID": "${localEnv:FACEBOOK_APP_ID}",
    "FACEBOOK_APP_SECRET": "${localEnv:FACEBOOK_APP_SECRET}",
    "GOOGLE_CLIENT_ID": "${localEnv:GOOGLE_CLIENT_ID}",
    "GOOGLE_CLIENT_SECRET": "${localEnv:GOOGLE_CLIENT_SECRET}",
    "SENTRY_DSN": "${localEnv:SENTRY_DSN}",
    "DATADOG_API_KEY": "${localEnv:DATADOG_API_KEY}",
    "NEW_RELIC_LICENSE_KEY": "${localEnv:NEW_RELIC_LICENSE_KEY}",
    "SEGMENT_WRITE_KEY": "${localEnv:SEGMENT_WRITE_KEY}",
    "MIXPANEL_TOKEN": "${localEnv:MIXPANEL_TOKEN}",
    "AMPLITUDE_API_KEY": "${localEnv:AMPLITUDE_API_KEY}",
    "CLOUDFLARE_API_KEY": "${localEnv:CLOUDFLARE_API_KEY}",
    "CLOUDFLARE_API_TOKEN": "${localEnv:CLOUDFLARE_API_TOKEN}",
    "S3_ACCESS_KEY": "${localEnv:S3_ACCESS_KEY}",
    "S3_SECRET_KEY": "${localEnv:S3_SECRET_KEY}",
    "S3_BUCKET": "${localEnv:S3_BUCKET}",
    "S3_REGION": "${localEnv:S3_REGION}",
    "GOOGLE_MAPS_API_KEY": "${localEnv:GOOGLE_MAPS_API_KEY}",
    "MAPBOX_TOKEN": "${localEnv:MAPBOX_TOKEN}",
    "ALGOLIA_APP_ID": "${localEnv:ALGOLIA_APP_ID}",
    "ALGOLIA_API_KEY": "${localEnv:ALGOLIA_API_KEY}",
    "SHOPIFY_API_KEY": "${localEnv:SHOPIFY_API_KEY}",
    "SHOPIFY_API_SECRET": "${localEnv:SHOPIFY_API_SECRET}",
    "WOO_CONSUMER_KEY": "${localEnv:WOO_CONSUMER_KEY}",
    "WOO_CONSUMER_SECRET": "${localEnv:WOO_CONSUMER_SECRET}",
    "WORDPRESS_API_KEY": "${localEnv:WORDPRESS_API_KEY}",
    "CONTENTFUL_ACCESS_TOKEN": "${localEnv:CONTENTFUL_ACCESS_TOKEN}",
    "SANITY_PROJECT_ID": "${localEnv:SANITY_PROJECT_ID}",
    "SANITY_TOKEN": "${localEnv:SANITY_TOKEN}",
    "GHOST_API_KEY": "${localEnv:GHOST_API_KEY}",
    "ZENDESK_API_TOKEN": "${localEnv:ZENDESK_API_TOKEN}",
    "INTERCOM_ACCESS_TOKEN": "${localEnv:INTERCOM_ACCESS_TOKEN}",
    "FRESHDESK_API_KEY": "${localEnv:FRESHDESK_API_KEY}",
    "JIRA_API_TOKEN": "${localEnv:JIRA_API_TOKEN}",
    "CONFLUENCE_API_TOKEN": "${localEnv:CONFLUENCE_API_TOKEN}",
    "NOTION_API_KEY": "${localEnv:NOTION_API_KEY}",
    "AIRTABLE_API_KEY": "${localEnv:AIRTABLE_API_KEY}",
    "DROPBOX_ACCESS_TOKEN": "${localEnv:DROPBOX_ACCESS_TOKEN}",
    "BOX_ACCESS_TOKEN": "${localEnv:BOX_ACCESS_TOKEN}",
    "GOOGLE_DRIVE_API_KEY": "${localEnv:GOOGLE_DRIVE_API_KEY}",
    "ZOOM_API_KEY": "${localEnv:ZOOM_API_KEY}",
    "ZOOM_API_SECRET": "${localEnv:ZOOM_API_SECRET}",
    "YOUTUBE_API_KEY": "${localEnv:YOUTUBE_API_KEY}",
    "VIMEO_ACCESS_TOKEN": "${localEnv:VIMEO_ACCESS_TOKEN}",
    "SPOTIFY_CLIENT_ID": "${localEnv:SPOTIFY_CLIENT_ID}",
    "SPOTIFY_CLIENT_SECRET": "${localEnv:SPOTIFY_CLIENT_SECRET}",
    "SOUNDCLOUD_CLIENT_ID": "${localEnv:SOUNDCLOUD_CLIENT_ID}",
    "WEATHER_API_KEY": "${localEnv:WEATHER_API_KEY}",
    "NEWS_API_KEY": "${localEnv:NEWS_API_KEY}",
    "ALPHA_VANTAGE_API_KEY": "${localEnv:ALPHA_VANTAGE_API_KEY}",
    "IEX_CLOUD_API_KEY": "${localEnv:IEX_CLOUD_API_KEY}",
    "CRYPTO_API_KEY": "${localEnv:CRYPTO_API_KEY}",
    "ETHERSCAN_API_KEY": "${localEnv:ETHERSCAN_API_KEY}",
    "INFURA_PROJECT_ID": "${localEnv:INFURA_PROJECT_ID}",
    "ALCHEMY_API_KEY": "${localEnv:ALCHEMY_API_KEY}",
    "MORALIS_API_KEY": "${localEnv:MORALIS_API_KEY}",
    "QUICKNODE_API_KEY": "${localEnv:QUICKNODE_API_KEY}",
    "PRIVATE_KEY": "${localEnv:PRIVATE_KEY}",
    "PUBLIC_KEY": "${localEnv:PUBLIC_KEY}",
    "SECRET_KEY": "${localEnv:SECRET_KEY}",
    "SIGNING_KEY": "${localEnv:SIGNING_KEY}",
    "ENCRYPTION_KEY": "${localEnv:ENCRYPTION_KEY}",
    "JWT_SECRET": "${localEnv:JWT_SECRET}",
    "SESSION_SECRET": "${localEnv:SESSION_SECRET}",
    "OAUTH_CLIENT_ID": "${localEnv:OAUTH_CLIENT_ID}",
    "OAUTH_CLIENT_SECRET": "${localEnv:OAUTH_CLIENT_SECRET}",
    "LICENSE_KEY": "${localEnv:LICENSE_KEY}",
    "ACTIVATION_KEY": "${localEnv:ACTIVATION_KEY}",
    "ADMIN_PASSWORD": "${localEnv:ADMIN_PASSWORD}",
    "ROOT_PASSWORD": "${localEnv:ROOT_PASSWORD}",
    "MASTER_KEY": "${localEnv:MASTER_KEY}",
    "API_RATE_LIMIT": "${localEnv:API_RATE_LIMIT}",
    "API_TIMEOUT_MS": "${localEnv:API_TIMEOUT_MS}"
  }
}